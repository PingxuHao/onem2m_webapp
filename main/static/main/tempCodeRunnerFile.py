time.sleep(1)
        # # Append and publish a new message
        # update_message = trimmed_message + "\n searching"
        # trimmed_message = trim_log_message(update_message)
        # client.publish(MQTT_TOPIC_LOG, trimmed_message)
